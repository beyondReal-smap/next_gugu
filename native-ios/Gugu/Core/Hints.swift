import Foundation

// 학습 로드맵 순서 (hints.ts 이식)
// 원본은 content/learning-path.json steps 순서의 읽기 전용 사본이다.
// 힌트 문구(hintForTable)와 보호자 팁(parentTipOfWeek)은 이를 쓰는 화면을 이식할 때 함께 옮긴다.

enum Hints {
    static let roadmapTables = [2, 5, 3, 4, 6, 9, 7, 8]
}
